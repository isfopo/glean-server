# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=*

# JWT Secret (for production use)
# JWT_SECRET=your-super-secret-jwt-key

# Rate Limiting (for future use)
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
# RATE_LIMIT_MAX_REQUESTS=100

# Environment Configuration
HOST="localhost"       # Hostname for the server
PUBLIC_URL=""          # Set when deployed publicly, e.g. "https://mysite.com". Informs OAuth client id.

# Database Configuration (for future use)
DB_PATH="./data"     # The SQLite database path. Leave as ":memory:" to use a temporary in-memory database.

# Secrets
# Must set this in production. May be generated with `openssl rand -base64 33`
COOKIE_SECRET="dev-only-secret"
